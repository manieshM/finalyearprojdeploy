Place the PostgreSQL JDBC driver jar in this folder before enabling database mode.

Recommended file:
- postgresql-42.x.x.jar

The app stays in local TSV mode unless these environment variables are set:
- APP_DB_URL
- APP_DB_USER
- APP_DB_PASSWORD
